<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!--ac全面屏幕设置，去掉状态栏等-->
    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <!--全屏显示，防黑屏-->
    <style name="AllAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <!--"android:windowIsTranslucent"能达到Acitivity透明效果，Activity A启动之后，启动Activity B，Activity A的onStop不会被调用。-->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <!--全屏显示，防黑屏-->
    <style name="ChatAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/indicator_uncheck</item>
    </style>
    <!--button椭圆背景-->
    <style name="FrameButtonStyle" parent="android:TextAppearance.Holo.Widget.Button">
        <item name="android:textColor">@color/colorApp</item>
        <item name="android:background">@drawable/btn_guide_bg</item>
        <item name="android:gravity">center</item>
        <item name="android:minWidth">120dp</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:paddingBottom">6dp</item>
    </style>
    <!--长按linerlayout-->
    <style name="content_item">
        <item name="android:background">@drawable/to_alpha</item>
    </style>

    <style name="content_item_white" parent="content_item">
        <item name="android:background">@drawable/ease_chat_press_speak_btn</item>
    </style>
    <!--VR查看大图ac-->
    <style name="TranslucentTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
    <!--AR-->
    <style name="SampleAppsTheme" parent="@android:style/Theme.Light.NoTitleBar.Fullscreen">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/black</item>
    </style>
</resources>
